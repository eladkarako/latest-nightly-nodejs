# latest-nightly-nodejs
[NodeJS][CMD] batch-file to get the URLs of the latest NodeJS resources from https://nodejs.org/download/nightly/ . STDOUT will have the version which you can then use to "build the URL" and use wGet to Aria2C to download. STDERR includes some examples and tips (you may ignore...)
